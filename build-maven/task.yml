platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: 3-jdk-11

inputs:
  - name: source

params:
  SRC_PATH:

run:
  path: sh
  args:
  - -exc
  - |
     cd source/${SRC_PATH}
     mvn build
