platform: linux

image_resource:
  type: docker-image
  source:
    repository: php
    tag: 7.1-apache

inputs:
  - name: source


outputs:
  - name: compile

run:
  path: bash
  args:
  - -exc
  - |
     cd source
     ./composer.phar install
     cd ../
     cp -a source/. compile
