platform: linux

image_resource:
  type: docker-image
  source:
    repository: nimmis/apache-php7

inputs:
  - name: source


outputs:
  - name: full-source

run:
  path: bash
  args:
  - -exc
  - |
     cd source
     composer.phar install
     cd ../
     cp -a source/. full-source
