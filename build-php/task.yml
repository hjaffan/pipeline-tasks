platform: linux

image_resource:
  type: docker-image
  source:
    repository: hjaffan/php
    label: latest

inputs:
  - name: source


outputs:
  - name: compile

run:
  path: bash
  args:
  - -exc
  - |
     cd source
     ./composer.phar install
     cd ../
     cp -a source/. compile
